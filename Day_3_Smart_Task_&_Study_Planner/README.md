This is a command-line Smart Task and Study Planner built with Python.

It allows users to create tasks with priorities and deadlines, track study time, and detect overdue tasks automatically.

All tasks and sessions are saved in JSON files so data persists between runs.

